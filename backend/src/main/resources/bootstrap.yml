spring:
  application:
    name: ai-manager
  cloud:
    nacos:
      config:
        server-addr: ${NACOS_ADDR:122.51.136.129:8848}
        file-extension: yaml
        username: ${NACOS_USERNAME:nacos}
        password: ${NACOS_PASSWORD:nacos}
      discovery:
        server-addr: ${NACOS_ADDR:122.51.136.129:8848}
        username: ${NACOS_USERNAME:nacos}
        password: ${NACOS_PASSWORD:nacos}
